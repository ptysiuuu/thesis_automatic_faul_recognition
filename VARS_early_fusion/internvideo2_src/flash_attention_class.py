class FlashAttention(object):
    """Stub — only instantiated when use_flash_attn=True, which we never set."""
    def __init__(self, *args, **kwargs):
        raise RuntimeError("Flash attention stub called. Set use_flash_attn=False.")
